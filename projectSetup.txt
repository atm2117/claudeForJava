E-commerce project:
  Browse through a List of product.
  Being able to add a product with a price.
  The backend will be a springboot restful api.
  The frontend will be built using NextJS.

Create the project using Spring Initializr:
  groupId: com.arnold.ecommerce
  artifactId: ecommerce-api
  packaging type: jar.
  springboot dependency: Spring web, Spring Data JPA, MySQL Driver SQL , Validation I/O, Spring Boot Actuator and lombok
  SQL:
    From the downloaded resources (C:\Users\arnol\Downloads\resources.zip\resources\project).
    Copy the schema.sql and added into a new 'db' folder under your project.
    Download MySQL workbench
    Download MySQL Server
    Copy the content of schema.sql into MySQL workbench and execute it.

Provide Context through CLAUDE.md and Rules:
  From the downloaded resources (C:\Users\arnol\Downloads\resources.zip\resources\global).
    Copy the CLAUDE.md
    Navigate to the .claude folder under my user folder (C:\Users\arnol).
    Paste the CLAUDE.md at this location and open it in our text editor.
    The md file contains instruction and constraints for specific section. Sections are identified by ##.
  From the downloaded resources (C:\Users\arnol\Downloads\resources.zip\resources\project).
    Copy the CLAUDE.md
    In VS code, at the project level, create a folder named '.claude' and paste the CLAUDE.md file.
  Create a new folder in VS Code under the .claude folder and name it rules.
  From the downloaded resources (C:\Users\arnol\Downloads\resources.zip\resources\rules).
    Copy api-design.md and testing.md
    Paste them in rules folder in VS Code.
  Check if claude understand our md files by asking the question below:
    1. What annotation do we use for dependency injection?
    2. What type do we use for money field?
    3. What should the API error response look like according to standard conventions?
  Every time we open a new session, claude will load all the rules present in our md files.
  Each time an md file is updated, the session need to be restarted for the change to be picked up.
  We placed our rules at the project level and inside the .claude folder. These are 2 different method.
    In general, we should pick one. The convention is to have the rules at the project level.

Auto Memory: Claude's Personal Notebook
  All the projects using claude can be found at this location: C:\Users\arnol\.claude\projects.
  In vs code, ask the prompt below?
    What do you have in memory for this project?
  Claude doesn't save data by itself. We can manually ask it to store information.

Git from Day 1:
  Setup git before any Java code is written. Not at the end of the course.
  Use claude to add our project to github with the name: 'my-java-app'
    C:\Users\arnol\OneDrive\Documents\projects\claudeAI\my-java-app
  Prompt:
    Review the existing .gitignore for ecommerce-api.
    Make sure it covers everything that should be ignored for this project:
    Spring Boot, Maven, MySQL, VS Code, and Claude Code.
    Add anything missing. Show exactly what you added and why.

MediTrack Assignment project walkthrough:
  From the downloaded resources (C:\Users\arnol\Downloads\resources.zip\resources\Assignments).
    Open the MediTrack_Prompt_Handbook.pdf file.
    Open the meditrack=starter in a new VS Code window.
    Copy the sql from meditrack_schema into MySql Workbench and run it.
    Make sure to understand what the project is currently doing. We can use claude for a sample explanation.
  Prompt:
    Create a file explaining what the application is currently doing using bullet points.
    The result has been save in APPLICATION_OVERVIEW.md

Assignment: Project Conventions Using Init Command
  Project path: C:\Users\arnol\Downloads\resources\resources\Assignments\meditrack-starter\
  In claude, generate the CLAUDE.md with the /init command
  Tighten the generated CLAUDE.md using the prompt:
    Update CLAUDE.md so it requires, for this project:
      - constructor injection with final fields (never @Autowired)
      - BigDecimal for money (never double)
      - DTOs in and out of controllers (never expose JPA entities)
      - thin controllers; all business logic in a service layer
      - all endpoints under /api/v1
      - centralised exception handling returning a consistent ErrorResponse JSON
      - ddl-auto=validate (tables come from meditrack_schema.sql)
  Ask few prompt question to claude:
    1. What do we use for dependency injection?
    2. What type do we use for money fields?

Role Play: Convincing a Skeptical Senior Developer to Adopt CLAUDE.md
