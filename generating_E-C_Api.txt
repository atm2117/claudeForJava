Prompt 1: Data Layer:
  file: ECom_Prompt_Handbook.pdf
  Project path: C:\Users\arnol\OneDrive\Documents\projects\claudeAI\my-java-app
  Generate the data layer using the prompt below:
    Generate the complete data layer for ecommerce-api. Read db/schema.sql first.
    Application config:
    - MySQL at localhost:3306/ecommerce_db, username root, password [update_with_real_password]
    - ddl-auto=validate (tables already exist), SQL logging on
    Three enums needed:
    OrderStatus: PENDING, CONFIRMED, PROCESSING, SHIPPED, DELIVERED, CANCELLED,
    REFUNDED
    PaymentMethod: CREDIT_CARD, DEBIT_CARD, UPI, NET_BANKING, WALLET,
    CASH_ON_DELIVERY
    PaymentStatus: PENDING, COMPLETED, FAILED, REFUNDED
    Two entity business rules:
    - Order number is generated on save as 'ORD-' followed by 8 uppercase UUID
    characters
    - OrderItem subtotal is calculated automatically as unit price × quantity
    - Saving an order must cascade to its items and payment
  Verify that the repository and the application.yml got created with correct content.
  Commit the code using claude or the terminal.

Prompt2: Business Layer:
  Generate the service and controller layers for ecommerce-api.
    Three business rules to apply:
    - Stock reduces when an order is placed, not at shipment
    - Cancelling an order restores stock for all items;
    if payment was completed, mark it REFUNDED
    - Order status transitions are one-way:
    PENDING → CONFIRMED or CANCELLED
    CONFIRMED → PROCESSING or CANCELLED
    PROCESSING → SHIPPED or CANCELLED
    SHIPPED → DELIVERED
    DELIVERED, CANCELLED, and REFUNDED are final
  Once it's done, use a prompt to 'run the app'.
  Test the products endpoint: localhost:8080/api/v1/products
  Check the actuator: localhost:8080/actuator/health
  Commit all the changes.

Write and Run Unit Tests:
  Prompts:
    Generate unit tests for all four services.
    Additional business scenarios to cover:
      - Category create: duplicate name
      - Product create: duplicate SKU
      - Customer register: duplicate email
      - Order placeOrder: customer not found, insufficient stock
      - Order cancelOrder: already delivered, already cancelled
    Verify that test were created and they are working.
    Commit the code.

Configure Swagger UI:
  Prompt:
    Add interactive Swagger UI to ecommerce-api using the latest springdoc-openapi dependency.
